# Lampa Offline MVP

Плагин добавляет в Lampa сохранение прямых видеофайлов и незашифрованных HLS-потоков для просмотра без интернета.

> Используйте плагин только для контента, который разрешено сохранять. Он не обходит DRM, шифрование и защиту видеосервисов.

## Установка версии 0.2.5

Версия 0.2.5 исправляет выбор действий и запуск сохранённого видео на iPhone.

Если установлена предыдущая версия, сначала удалите её из списка расширений Lampa. Одновременно оставлять несколько версий нельзя.

1. Откройте **Lampa → Настройки → Расширения**.
2. Удалите старый адрес офлайн-плагина.
3. Нажмите **Добавить плагин**.
4. Вставьте адрес:

```text
https://cdn.jsdelivr.net/gh/iteaky/lampa@main/lampa-offline-mvp-0.2.5.js
```

5. Подтвердите добавление.
6. Полностью закройте Lampa, выгрузите её из памяти и откройте снова.

## Как пользоваться

1. Откройте фильм или сериал через онлайн-плагин.
2. Выберите перевод, качество или серию и запустите видео.
3. Перед открытием плеера появится меню:
   - **Смотреть**;
   - **Сохранить офлайн**;
   - **Смотреть и сохранить**.
4. Прогресс отображается в разделе **Офлайн**.

## Действия с загрузкой

На iPhone коснитесь карточки загрузки. На телевизоре или компьютере выделите запись и нажмите **OK / Enter**.

Для готовой записи доступны:

- **Смотреть офлайн**;
- **Удалить**.

Для загрузки со статусом **В очереди** или **Загрузка** доступны:

- **Отменить и удалить**;
- **Удалить**.

Для записи со статусом **Ошибка** или **Отменено** доступны:

- **Повторить загрузку**;
- **Удалить**.

## Что исправлено в 0.2.5

- выбранная карточка определяется напрямую по её `data-id`, а не через текущий фокус Lampa;
- после выбора пункта **Смотреть офлайн** плагин больше не пытается повторно найти карточку;
- MP4/WebM/MKV читаются напрямую из IndexedDB и передаются плееру как локальный `blob:` URL;
- локальный HLS собирается напрямую из сохранённых фрагментов и запускается через Hls.js;
- после закрытия плеера временные `blob:` URL освобождаются;
- повторные тапы по списку продолжают работать после закрытия меню или плеера;
- свайп для прокрутки не считается нажатием;
- сохранены отмена и удаление недогруженных записей;
- стабильный файл `lampa-offline-mvp.js` переключён на 0.2.5.

## Возможности версии 0.2.x

- прямые MP4, WebM и MKV;
- незашифрованные HLS-потоки `.m3u8`;
- выбор варианта с наибольшим `BANDWIDTH`;
- загрузка HLS-плейлиста и видеофрагментов;
- поддержка незашифрованного `EXT-X-MAP`;
- локальное воспроизведение через Hls.js;
- очередь, прогресс, повтор, отмена и удаление загрузок.

## Ограничения

Не поддерживаются:

- DRM/Widevine;
- HLS с `EXT-X-KEY` и шифрованием;
- HLS с `EXT-X-BYTERANGE`;
- MPEG-DASH (`.mpd`);
- отдельная внешняя аудиодорожка из сложного master-плейлиста;
- источники, требующие специальных HTTP-заголовков;
- загрузка при полностью закрытой Lampa;
- продолжение с точного байта после закрытия приложения.

Ответ `423 Locked` приходит от самого видеосервера. Такую ссылку нельзя скачать до получения новой рабочей ссылки от онлайн-плагина.

## Диагностика

После запуска версии 0.2.5 в консоли должны появиться строки:

```text
[Offline HLS direct] direct touch and click actions installed 0.2.5
[Offline HLS direct] direct fetch compatibility installed
[Offline HLS direct] initialized 0.2.5
```

После тапа по записи:

```text
[Offline HLS direct] item selected directly by touch
```

После выбора **Смотреть офлайн**:

```text
[Offline HLS direct] playing item directly
```

## Файлы

- `lampa-offline-mvp-0.2.5.js` — прямой выбор карточки и локальное воспроизведение;
- `lampa-offline-mvp-0.2.3.js` — основное совместимое меню и отмена загрузок;
- `lampa-offline-mvp-0.2.0.js` — ядро загрузки MP4/HLS;
- `lampa-offline-mvp.js` — стабильный загрузчик актуальной версии;
- `CHANGELOG.md` — история изменений.

## Проверка синтаксиса

```bash
node --check lampa-offline-mvp-0.2.5.js
node --check lampa-offline-mvp-0.2.3.js
node --check lampa-offline-mvp-0.2.0.js
```

## Лицензия

MIT. См. [LICENSE](LICENSE).
